apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "localedit.name" . }}-test-connection"
  labels:
    {{- include "localedit.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['-qO-', 'http://{{ include "localedit.name" . }}:{{ .Values.service.port }}/']
  restartPolicy: Never
